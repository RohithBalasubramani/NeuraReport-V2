// DataTable remains in components/data.jsx (too large to extract in Phase 2).
// This barrel re-exports it so the shared/ hierarchy is complete.
// NOTE: components/data.jsx is NOT a bridge -- it retains its original code.
export { DataTable } from '@/components/data'
