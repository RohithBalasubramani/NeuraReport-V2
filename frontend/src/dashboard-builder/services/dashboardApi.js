/**
 * Dashboard Builder API service layer.
 * Centralizes all API calls used by the dashboard-builder domain.
 */
export { getWidgetData, getWidgetReportData, packGrid, selectWidgets } from '@/api/monitoring'
