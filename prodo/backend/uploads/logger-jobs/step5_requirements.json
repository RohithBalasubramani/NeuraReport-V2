{
  "datasets": {
    "header": {
      "description": "Report header scalars: reporting period dates and generation timestamp, all supplied as runtime parameters.",
      "columns": [
        "date_from",
        "date_to",
        "generated_at"
      ]
    },
    "rows": {
      "description": "One row per configured data collection job from app_jobs. Columns: name, type, status, enabled flag, polling interval in milliseconds, and CPU budget tier.",
      "columns": [
        "row_name",
        "row_type",
        "row_status",
        "row_enabled",
        "row_interval_ms",
        "row_cpu_budget"
      ],
      "grouping": [],
      "ordering": [
        "app_jobs.name ASC"
      ]
    },
    "totals": {
      "description": "No totals section defined for this report.",
      "columns": []
    }
  },
  "semantics": "date_from and date_to are display-only label parameters — they are NOT used to filter rows. All jobs are listed regardless of date. generated_at is a server-side runtime injection (current datetime).",
  "parameters": {
    "required": [
      {
        "name": "date_from",
        "type": "date"
      },
      {
        "name": "date_to",
        "type": "date"
      },
      {
        "name": "generated_at",
        "type": "string"
      }
    ],
    "optional": []
  },
  "transformations": [
    "No reshape required: all row tokens map directly to columns in app_jobs with a single NONE strategy."
  ]
}