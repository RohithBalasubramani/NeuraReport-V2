{
  "datasets": {
    "header": {
      "description": "Report header scalars: date range supplied by the user and a runtime-generated timestamp. No database lookup required for header fields.",
      "columns": [
        "date_from",
        "date_to",
        "generated_at"
      ]
    },
    "rows": {
      "description": "One row per device record from app_devices. Includes device identity, protocol, connectivity status, port, latency, auto-reconnect flag, and last error string.",
      "columns": [
        "row_id",
        "row_name",
        "row_protocol",
        "row_status",
        "row_port",
        "row_latency_ms",
        "row_auto_reconnect",
        "row_last_error"
      ],
      "grouping": [],
      "ordering": [
        "app_devices.id ASC"
      ]
    },
    "totals": {
      "description": "No totals section in this template.",
      "columns": []
    }
  },
  "semantics": "date_from and date_to are pass-through parameters displayed in the report header and footer; they do not filter rows from app_devices. generated_at is a runtime-injected value (current timestamp at generation time) and has no catalog source.",
  "parameters": {
    "required": [
      {
        "name": "date_from",
        "type": "date"
      },
      {
        "name": "date_to",
        "type": "date"
      }
    ],
    "optional": []
  },
  "transformations": [
    "Select all columns directly from app_devices — no reshape, no pivot, no union.",
    "Format row_latency_ms to 1 decimal place for display.",
    "Order rows by app_devices.id ascending.",
    "Inject generated_at as the current report generation timestamp at runtime."
  ]
}