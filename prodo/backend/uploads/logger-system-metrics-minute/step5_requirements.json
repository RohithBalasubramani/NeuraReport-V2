{
  "datasets": {
    "header": {
      "description": "Three scalar values for the report header: date range bounds and report generation timestamp. date_from and date_to come from user parameters; generated_at is injected at runtime.",
      "columns": [
        "date_from",
        "date_to",
        "generated_at"
      ]
    },
    "rows": {
      "description": "One row per UTC minute from app_metrics_system_minute, filtered to the requested date range. Each row contains CPU %, memory %, disk read/write rates, network RX/TX rates, and process-level CPU, RSS, and handle count.",
      "columns": [
        "row_minute_utc",
        "row_cpu",
        "row_mem",
        "row_disk_rps",
        "row_disk_wps",
        "row_net_rxps",
        "row_net_txps",
        "row_proc_cpu",
        "row_proc_rss_mb",
        "row_proc_handles"
      ],
      "grouping": [],
      "ordering": [
        "minute_utc ASC"
      ]
    },
    "totals": {
      "description": "No totals section defined in this template.",
      "columns": []
    }
  },
  "semantics": "date_from and date_to are required user-supplied filter parameters that bound the minute_utc column. generated_at is a pass-through runtime scalar not derived from any database column.",
  "parameters": {
    "required": [
      {
        "name": "date_from",
        "type": "date"
      },
      {
        "name": "date_to",
        "type": "date"
      },
      {
        "name": "generated_at",
        "type": "string"
      }
    ],
    "optional": []
  },
  "transformations": [
    "No reshape required — all row tokens map directly to columns in app_metrics_system_minute with a NONE strategy.",
    "Filter app_metrics_system_minute rows to those where minute_utc is between date_from and date_to inclusive.",
    "Order result rows by minute_utc ascending to present chronological telemetry."
  ]
}