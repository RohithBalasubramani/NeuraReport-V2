{
  "datasets": {
    "header": {
      "description": "Report header scalars: date range boundaries and generation timestamp. date_from and date_to come from user parameters; generated_at is injected by the runtime at report generation time.",
      "columns": [
        "date_from",
        "date_to",
        "generated_at"
      ]
    },
    "rows": {
      "description": "One row per notification record from app_notifications, filtered by the time column within the selected date range. Displays notification ID, type, message, user, read status, and timestamp.",
      "columns": [
        "row_id",
        "row_type",
        "row_message",
        "row_user",
        "row_read",
        "row_time"
      ],
      "grouping": [],
      "ordering": [
        "app_notifications.time DESC"
      ]
    },
    "totals": {
      "description": "No totals section in this report.",
      "columns": []
    }
  },
  "semantics": "date_from and date_to are required filter parameters that bound the query on app_notifications.time. generated_at is a pass-through runtime scalar (current datetime) and does not filter or join any table.",
  "parameters": {
    "required": [
      {
        "name": "date_from",
        "type": "date"
      },
      {
        "name": "date_to",
        "type": "date"
      }
    ],
    "optional": [
      {
        "name": "generated_at",
        "type": "string"
      }
    ]
  },
  "transformations": [
    "No reshape needed — data is a flat single-table query from app_notifications.",
    "Filter rows where app_notifications.time is between date_from and date_to.",
    "Order results by app_notifications.time descending so the most recent notifications appear first."
  ]
}