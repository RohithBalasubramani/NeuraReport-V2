{
  "datasets": {
    "header": {
      "description": "Three scalar values: reporting period start, end, and report generation timestamp. date_from and date_to come from user-supplied parameters; generated_at is injected at runtime as the current UTC timestamp.",
      "columns": [
        "date_from",
        "date_to",
        "generated_at"
      ]
    },
    "rows": {
      "description": "One row per (job_id, minute_utc) pair from app_metrics_jobs_minute, filtered to the requested reporting window. Each row carries read/write counts, error counts, and latency percentile values.",
      "columns": [
        "row_job_id",
        "row_minute_utc",
        "row_reads",
        "row_read_err",
        "row_writes",
        "row_write_err",
        "row_read_p50",
        "row_read_p95",
        "row_write_p50",
        "row_write_p95"
      ],
      "grouping": [],
      "ordering": [
        "job_id ASC",
        "minute_utc ASC"
      ]
    },
    "totals": {
      "description": "No totals section defined in this template.",
      "columns": []
    }
  },
  "semantics": "date_from and date_to are required user-supplied filter parameters that bound the minute_utc range returned. generated_at is a pass-through parameter injected by the report runner at generation time (not stored in the database).",
  "parameters": {
    "required": [
      {
        "name": "date_from",
        "type": "date"
      },
      {
        "name": "date_to",
        "type": "date"
      },
      {
        "name": "generated_at",
        "type": "string"
      }
    ],
    "optional": []
  },
  "transformations": [
    "No reshape required — all row data comes from a single flat table (app_metrics_jobs_minute).",
    "Filter rows to minute_utc between date_from and date_to (inclusive).",
    "Order results by job_id ascending, then minute_utc ascending.",
    "Inject generated_at as a runtime scalar (current UTC timestamp) at report generation time."
  ]
}