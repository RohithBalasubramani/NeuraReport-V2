## Coastal Consumption Report

- Presents plant/location header plus optional recipe filter spanning the provided date window.
- Unpivots bin1..bin12 columns from recipes to display material-wise set vs achieved weights for every batch.
- Error columns reflect achieved minus set weight, with totals aggregating across the filtered population.
- Contract tokens map directly to recipes.* columns so discovery/report/fill flows can resolve key filters.
