/**
 * Scheduler API service layer.
 */
export {
  listSchedules,
  createSchedule,
  updateSchedule,
  deleteSchedule,
  triggerSchedule,
  getSchedulerStatus,
  listApprovedTemplates,
} from '@/api/client'
